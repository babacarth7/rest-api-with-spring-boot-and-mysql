spring:
  application:
    name: taskmanager
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/task_manager_db
    username: root
    password: ''
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
      generate-ddl: true
server:
  port: 8085
